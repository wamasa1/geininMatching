<!DOCTYPE html>
<html lang="ja">
  <body>
    <div class="container text-center">
      <p>{{$text}}</p>
      <a class="btn btn-danger" href="{{ url('/messagebox') }}">メッセージボックスへ</a>
    </div>
  </body>
</html>
